# Core
numpy>=1.24
pandas>=2.0
pyyaml>=6.0
tqdm>=4.65
pillow>=10.0
opencv-python>=4.8
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.13

# Face detection / landmarks (used by classical manipulations)
# Pin <0.10.20 because newer versions on some platforms drop the `solutions` API,
# and the Tasks-API fallback requires a model download that may be blocked on
# locked-down networks. The framework still works with newer versions IF the
# `face_landmarker.task` file is reachable (or if dlib is installed).
mediapipe>=0.10.13,<0.10.20
dlib>=19.24            # Used as fallback / for 68-point landmarks.

# Models
deepface>=0.0.93
insightface>=0.7
onnxruntime-gpu>=1.16  # If CPU-only, replace with onnxruntime.
torch>=2.1
torchvision>=0.16
transformers>=4.40
timm>=1.0              # Required by some HF age models.

# Reporting
jinja2>=3.1
